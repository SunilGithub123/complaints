# ArchUnit configuration.
# During scaffold, the per-module packages (controller, service, repository, model)
# do not yet exist, so the rules in PackageBoundaryTest match zero classes.
# That's expected — flip this back to `true` once the first module lands and the
# rules begin matching real packages.
archRule.failOnEmptyShould=false

